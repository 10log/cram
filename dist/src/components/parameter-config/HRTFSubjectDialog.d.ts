import { default as React } from 'react';
export declare const HRTFSubjectDialog: ({ open, onClose, selectedId, onSelect, }: {
    open: boolean;
    onClose: () => void;
    selectedId: string;
    onSelect: (id: string) => void;
}) => React.JSX.Element;
export default HRTFSubjectDialog;
